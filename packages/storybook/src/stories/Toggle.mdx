import { Meta, Canvas } from '@storybook/blocks'
import * as Toggle from "./Toggle.stories";

<Meta title="Toggle" name="Docs" of={Toggle} />

# Toggle

#### Description
모바일 앱 전용 Toggle 컴포넌트.\
옵셔널 파라미터(`color`)를 통해 색상을 고정하여 사용 가능.\
`color` 데이터는 css var(...) 혹은 HEX 코드(#...) 형태로 사용.

<Canvas of={Toggle.Default} />
<Canvas of={Toggle.Disabled} />
