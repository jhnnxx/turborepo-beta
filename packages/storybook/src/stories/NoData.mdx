import { Meta, Canvas } from '@storybook/blocks'
import * as NoData from './NoData.stories'

<Meta title="NoData" name="Docs" of={NoData} />

# NoData

### Props

<table>
  <thead>
    <tr>
      <th>isPayment</th>
      <th>className</th>
      <th>message</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>boolean(default: false)</td>
      <td>string(default: '검색 결과가 없습니다.')</td>
      <td>string</td>
    </tr>
  </tbody>
</table>

#### Description

- isPayment: 주문 배송 조회에 사용할때 추가 한다. true 일때 message는 '최근 주문 내역이 없습니다.' 으로 보인다.
- message는 기본값으로 '검색 결과가 없습니다.'으로 표시 되며, 필요에 따라 message 값을 추가하면 된다.

<Canvas of={NoData.noData} />
<Canvas of={NoData.noDataMessage} />
<Canvas of={NoData.noDataPayment} />
